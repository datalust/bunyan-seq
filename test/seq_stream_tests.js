"use strict";

let SeqStream = require('../seq_stream');

describe('SeqStream', () => {
   describe('constructor', () => {
       it('can be constructed', () => {
          new SeqStream(); 
       });
   });
});